import React, { Component } from 'react';

/**
 * Currently not being used but could be
 * useful in the future if teams are allowed on
 * the platform. It would show information about a team
 * to participants.
 */
class TeamProfile extends Component {

    render() {
        return (
            <div>
                Go Team!
            </div>
        );
    }
}

export default TeamProfile;